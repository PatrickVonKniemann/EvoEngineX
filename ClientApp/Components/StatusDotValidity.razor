@code {
    [Parameter]
    public bool IsValid { get; set; }

    private string GetDotColor()
    {
        return IsValid ? "bg-green-500" : "bg-red-500";
    }
}

<div class="flex items-center space-x-2">
    <span class="block w-2 h-2 @GetDotColor() rounded-full"></span>
</div>